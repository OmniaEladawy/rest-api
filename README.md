# Rest API using node js and mongodb

```
Rest API that confirms to REST architectural style for these models:
    > Article (title, body, comments, date)
        where comments field is an array of Comment (content, username, date)

    > Users (username, dob, isSuspended: false)

- use HATEOAS (for root /)
```
